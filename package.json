{
  "name": "capture-the-bugs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Exploratory testing exercise. The site itself needs no build step; npm is only here for the test suite.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
