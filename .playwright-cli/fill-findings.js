async (page) => {
  const findings = ["doesn't scroll", "\"to\" is not included in highlight", "License test too small", "Why is license test an image", "Scrolling breaks after super long input", "Horizontally breaks when too long word", "mobile layout broken", "you're doesnt trigger", "Whitespace only (spaces, a tab, blank lines) counts word count to 1", "Write forbidden verbs into own lines, word count and discouraged words count not working.\n\n<example>\nbe\nbeing\nbeen\nam\nis\nare\nwas\nwere\n</example>", "| B01 | S1 | Parser | Newline does not end a word: last word of a line merges with first word of next line, so to-be verbs at line ends escape detection | Enter `I am` + Enter + `happy`, click Check | `am` flagged, 3 words | Output `I` / `amhappy`, nothing flagged, 2 words. Root cause: `c=''` after `\\n` and `\"...\".indexOf('')` returns 0, so the empty char is appended to the current word instead of flushing it | A1, A2 |", "1. **Line breaks merge words and hide violations.** `This is⏎what I want` gets checked as \"iswhat\" with 0 discouraged words. `is⏎was` gives 0 violations. Windows line endings behave the same. The last word of each line also jumps into the next paragraph in the output.", "2. **Word count miscounts around whitespace.** A line break alone counts as 1 word, and `hello ⏎ world` counts 3. Tabs and non-breaking spaces do not separate words, so `hello⇥world` counts 1. Standalone punctuation counts as a word: `hello , world - test` counts 5 and `...` counts 1.", "3. **Single quotes hide discouraged words.** `'is'`, `is'` and `'am` are all unflagged, while `\"is\"` in double quotes is flagged. Inconsistent.", "4. **Curly apostrophes defeat detection.** `isn't it's I'm` gives 0 discouraged and 0 possible. The straight-apostrophe version gives 2 and 1. Text pasted from Word, Google Docs or a phone will usually have curly apostrophes.", "Hello", "Word Count ignores tab characters as word separators. Enter two words with a tab between them (\"a<TAB>b\") and Word Count reports 1 instead of 2. A lone tab reports 1 word instead of 0. Only the plain space character is treated as whitespace — a form feed behaves the same way. Violation detection still works correctly on tab-separated words, so only the headline Word Count number is wrong.", "A line break does not end the word before it, so words on either side of Enter merge into a single token and real violations disappear. Type \"am\", press Enter, type \"be\": Discouraged Words reports 0 instead of 2, and the output shows the merged word \"ambe\". Separately, the paragraph break is written BEFORE the pending word is flushed, so a word typed before a line break is displayed in the paragraph AFTER the break — \"am\" + Enter renders as an empty paragraph followed by \"am\". This breaks the core feature on any multi-line text, which is the normal way people use a textarea.", "Contractions ending in 're are never flagged as violations. Type \"you're\", \"we're\" or \"they're\" and Discouraged Words reports 0, even though 're can only ever contract the word \"are\". An entire class of to-be violations is missed: the word list holds only 15 literal forms (be, being, been, am, is, are, was, were, isn't, aren't, wasn't, weren't, ain't, i'm, amn't) and there is no rule for the 're word ending at all. By contrast the 's ending IS handled, so the omission looks accidental.", "The Possible Violations check for the 's ending looks at the FIRST apostrophe in the word instead of the word ending, so any word containing an earlier apostrophe is missed. \"John's\" correctly reports 1 Possible Violation, but \"O'Brien's\", \"y'all's\", \"o'clock's\" and \"rock'n'roll's\" all report 0. The code takes everything from the first apostrophe onward and compares it against \"'s\", so for \"O'Brien's\" it tests \"'brien's\" and finds no match. Single-apostrophe words work, which is why this stays hidden.", "The app only recognises the straight ASCII apostrophe ('), so contractions written with a curly apostrophe (') are completely invisible to it. Type \"isn't\" with a straight apostrophe and you get 1 Discouraged Word; type \"isn't\" with a curly one and you get 0. Same for \"it's\" — 1 Possible Violation straight, 0 curly. This matters enormously in practice because Word, Google Docs, iOS and macOS all autocorrect straight apostrophes to curly ones, so anyone pasting real prose from a word processor gets a clean bill of health on a document full of contractions. The curly apostrophe is treated as a word separator instead of a word character, so \"it's\" is read as the two separate words \"it\" and \"s\".", "Ampersands are not escaped in the output, so HTML entities typed by the user get decoded and the displayed text no longer matches what was typed. Type \"&amp;\" (5 characters) and the output shows a single \"&\". Type \"&lt;\" and the output shows \"<\". Type \"&nbsp;\" and it becomes a blank space. Less-than and greater-than ARE escaped, so this is display corruption rather than script injection — I verified in Chrome that typing \"&lt;b&gt;BOLD&lt;/b&gt;\" renders the literal text \"<b>BOLD</b>\" and creates no bold element, because a decoded entity is never re-parsed as a tag. Still a real bug: the tool silently alters the text you asked it to check.", "A line break immediately after a space invents a phantom word in the count. Press Enter on otherwise empty input: Word Count reports 1 instead of 0. Type \"a\", space, Enter, space, \"b\": Word Count reports 3 instead of 2. Every blank line between paragraphs silently adds one to the word count, so any normally paragraphed document over-counts. This is the opposite error from the tab bug (which under-counts), and it comes from the newline being blanked to an empty string that the word-boundary check then treats as the start of a new word.", "Line-break-heavy text is about 6x slower per character than ordinary prose, because every single newline emits a new paragraph element with no suppression of empty ones. Pasting 100,000 characters of line-broken text freezes the page for well over a second, synchronously inside the button's click handler, so the UI is unresponsive with no progress indication. Measured growth is linear rather than quadratic, so this is a constant-factor problem rather than an algorithmic blow-up, but the constant is bad enough to be user-visible: 10,000 newlines produces 10,001 paragraph elements. Ordinary prose of the same size is roughly 6x faster.", "A NUL character (U+0000) typed or pasted into the text is silently dropped from the output, so the echoed text no longer matches the input. Other control characters (U+0001, U+0007, U+001B) and the byte-order mark U+FEFF all survive intact — NUL is the only one that disappears. Counts are unaffected. Low impact and partly a consequence of writing the result through innerHTML, since the HTML parser discards NUL in character data, but it is still silent data loss in a tool whose job is to echo your text back to you faithfully.", "Two implementation smells that do not currently produce wrong answers but make the code fragile. First, the list of discouraged words is rebuilt from scratch inside the lookup function on EVERY word checked, rather than once per run — measured at about 25ns per word, a 1.27x overhead on that function. Second, that list is built as a JavaScript Array used as a string-keyed map, so it inherits every Array prototype property (length, constructor, toString, concat, sort...). Today those do not produce false positives, but only by luck: the membership test compares the looked-up value against the word itself, and no inherited value happens to equal its own name. Had it been written as a simple truthiness or \"in\" check, every one of those 40-odd built-in names would be flagged as a to-be violation. I tested all of them and they are clean — this is a latent trap rather than a live bug.", "counts too many words when i typed hi my name is rasmus and i work at ikea ", "does not recognize where the enter is", "the license picture is too small", "almost every single text on the page has a different font", "flagged the word a being which in this case refers to a living entity that exists", "mistakes a punctuation mark for a word", "works only for english", "mistakes a html tag or a script mark up for a word", "if i type bé it dooes not flagg it as an to-be word", "text is too large", "if there is a punctuation mark with a space on both sides it counts it as a word", "if you make larg text the system makes it small", "system cannot be operated by only a keyboard for example the check for E-prime button cannot be pressed with out a mouse", "the layout of the system looks like a powerpoint slideshow", "the system does not tell me if it is uncertain about some to-be words and just calls them violitions", "i cannot get any legal information about the systems use", "if you type over 10 000 words you cannot even see the check for e prime button so the system needs to be able to scroll down", "with multiple apothrophes it does not work", "counts emojis as words", "very long unbroken word goes off the screen\n", "does not recognze words that end with 're", "recognizes let's go as a to be word", "no button to clear\n", "the whole thing looks boring", "no way to go back to your previous texts", "the apache license 2.0 box is not a text it is a picture", "does not recognize slang words hidin to be words"];
  for (let i = 0; i < findings.length; i++) {
    const boxes = await page.getByRole('textbox', { name: 'Describe the bug you found...' }).all();
    const box = boxes[boxes.length - 1];
    await box.fill(findings[i]);
    if (i < findings.length - 1) {
      await page.getByRole('button', { name: '+ Add Finding' }).click();
    }
  }
  return { filled: findings.length };
}